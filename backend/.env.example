# MongoDB Atlas Connection String
MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/database?appName=Cluster0

# Server Port (optional, defaults to 5000)
PORT=5000

# Node Environment
NODE_ENV=production
